@model IEnumerable<TecBurguer.Models.HamburguerIgrediente>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuantidadeNecessario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdHamburguerNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdIngredienteNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuantidadeNecessario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdHamburguerNavigation.IdHamburguer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdIngredienteNavigation.IdIngrediente)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
